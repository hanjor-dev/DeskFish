# DeskFish Art Pack

当前目录是 Issue #2 的首个可用资源包。资源均为 `PLACEHOLDER` 占位质量 SVG，仅用于验证透明窗口纵切片、构图、资源命名和表现层接线；它们不是最终美术。

- 契约：`art-contract.json`
- 设计与导入约定：`docs/ART-DIRECTION.md`
- 资源不包含领域逻辑，不应被写入鱼只存档。
- 项目当前未安装 `com.unity.vectorgraphics`；正式 Sprite 导入可采用 Vector Graphics 或导出同名 PNG/WebP。
