---
status: accepted
---

# 采用离线优先的版本化快照存档

## 背景

DeskFish 的核心观赏和养成不能依赖网络；桌面程序也必须面对强制结束、系统休眠、时间离线和存档损坏。

## 决策

使用版本化完整快照保存用户、鱼只、资源、积分、时间和设置。保存时采用临时文件、校验、原子替换和上一份备份；启动时支持迁移和备份回退。离线时间按真实时间差计算，单次最多结算 24 小时，系统时间回拨不产生收益。

## 结果

本地体验不受网络影响，恢复路径明确，规则容易测试。代价是需要维护 schema 版本和迁移器；未来云同步必须处理冲突，不能简单覆盖本地状态。
